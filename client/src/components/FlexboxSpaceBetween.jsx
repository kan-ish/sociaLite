import { Box } from "@mui/material";
import { styled } from "@mui/system";

const FlexboxSpaceBetween = styled(Box)({
	display: "flex",
	justifyContent: "space-between",
	alignItems: "center",
});

export default FlexboxSpaceBetween;
